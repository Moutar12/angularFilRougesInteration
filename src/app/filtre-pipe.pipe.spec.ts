import { FiltrePipePipe } from './filtre-pipe.pipe';

describe('FiltrePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrePipePipe();
    expect(pipe).toBeTruthy();
  });
});
