import { ServiceDetailPromisePipe } from './service-detail-promise.pipe';

describe('ServiceDetailPromisePipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceDetailPromisePipe();
    expect(pipe).toBeTruthy();
  });
});
